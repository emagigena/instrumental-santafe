//import React from 'react'
//import PhotoCarousel from './PhotoCarousel'


export default function ListPhotosCarousel( {fotos} ) {
  return (
    { /*fotos.map ( (foto) => <Carousel.Item>
     <img 
     className="d-block w-100" 
     src={foto.foto} 
     alt="First slide"
     />
       </Carousel.Item>
     )
     <PhotoCarousel key={foto.idfoto} foto={foto} />)
    */}
    )
}